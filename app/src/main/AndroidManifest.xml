<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/calendar2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/calendar2_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sara_petrovic_rn4520"
        tools:targetApi="31">
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.DailyTasksActivity" />
        <activity android:name=".activities.TaskInfoActivity" />
        <activity
            android:name=".activities.BottomNavigationActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>